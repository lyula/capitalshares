<?php
// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/deposits.php');
	include_once(__DIR__ . '/deposits_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('deposits');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'deposits';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`deposits`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`username`), CONCAT_WS('',   `users1`.`username`), '') /* Username */" => "user_id",
		"`deposits`.`amount`" => "amount",
		"`deposits`.`phone_number`" => "phone_number",
		"if(`deposits`.`date`,date_format(`deposits`.`date`,'%d/%m/%Y'),'')" => "date",
		"`deposits`.`status`" => "status",
		"`deposits`.`mpesa_code`" => "mpesa_code",
		"`deposits`.`payment_refference`" => "payment_refference",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`deposits`.`id`',
		2 => '`users1`.`username`',
		3 => '`deposits`.`amount`',
		4 => 4,
		5 => '`deposits`.`date`',
		6 => 6,
		7 => 7,
		8 => 8,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`deposits`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`username`), CONCAT_WS('',   `users1`.`username`), '') /* Username */" => "user_id",
		"`deposits`.`amount`" => "amount",
		"`deposits`.`phone_number`" => "phone_number",
		"if(`deposits`.`date`,date_format(`deposits`.`date`,'%d/%m/%Y'),'')" => "date",
		"`deposits`.`status`" => "status",
		"`deposits`.`mpesa_code`" => "mpesa_code",
		"`deposits`.`payment_refference`" => "payment_refference",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`deposits`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`users1`.`username`), CONCAT_WS('',   `users1`.`username`), '') /* Username */" => "Username",
		"`deposits`.`amount`" => "Amount",
		"`deposits`.`phone_number`" => "Phone number",
		"`deposits`.`date`" => "Date",
		"`deposits`.`status`" => "Status",
		"`deposits`.`mpesa_code`" => "Mpesa code",
		"`deposits`.`payment_refference`" => "Payment refference",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`deposits`.`id`" => "id",
		"IF(    CHAR_LENGTH(`users1`.`username`), CONCAT_WS('',   `users1`.`username`), '') /* Username */" => "user_id",
		"`deposits`.`amount`" => "amount",
		"`deposits`.`phone_number`" => "phone_number",
		"if(`deposits`.`date`,date_format(`deposits`.`date`,'%d/%m/%Y'),'')" => "date",
		"`deposits`.`status`" => "status",
		"`deposits`.`mpesa_code`" => "mpesa_code",
		"`deposits`.`payment_refference`" => "payment_refference",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['user_id' => 'Username', ];

	$x->QueryFrom = "`deposits` LEFT JOIN `users` as users1 ON `users1`.`id`=`deposits`.`user_id` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = true;
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->AllowAdminShowSQL = 0;
	$x->RecordsPerPage = 100;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'deposits_view.php';
	$x->TableTitle = 'Deposits';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`deposits`.`id`';
	$x->DefaultSortField = '1';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Username', 'Amount', 'Phone number', 'Date', 'Status', 'Mpesa code', 'Payment refference', ];
	$x->ColFieldName = ['user_id', 'amount', 'phone_number', 'date', 'status', 'mpesa_code', 'payment_refference', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, 7, 8, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/deposits_templateTV.html';
	$x->SelectedTemplate = 'templates/deposits_templateTVS.html';
	$x->TemplateDV = 'templates/deposits_templateDV.html';
	$x->TemplateDVP = 'templates/deposits_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: deposits_init
	$render = true;
	if(function_exists('deposits_init')) {
		$args = [];
		$render = deposits_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: deposits_header
	$headerCode = '';
	if(function_exists('deposits_header')) {
		$args = [];
		$headerCode = deposits_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: deposits_footer
	$footerCode = '';
	if(function_exists('deposits_footer')) {
		$args = [];
		$footerCode = deposits_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
